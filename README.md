Чтобы запустить проект, скопируйте  .env.example  в  .env  и заполните своими данными».
Переменные из .env тянутся в docker-compose.yaml и profiles.yml (для dbt внутри папки analytics) 
Далее сборка через docker-compose. 